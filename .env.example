# Optional: override defaults from config/config.yaml
CIRC_CONFIG_PATH=config/config.yaml

# If you want the UI to load from a specific run directory (otherwise it picks latest)
CIRC_OUTPUT_RUN_DIR=

# Streamlit (optional)
STREAMLIT_SERVER_PORT=8501
